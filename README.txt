Daily Study Tracker - Static (Ready for direct deploy)

How to deploy on Vercel (upload ZIP):
1. Download and upload the ZIP to Vercel via 'Upload Project' on https://vercel.com/new
2. Make sure you upload the ZIP with files at the root (index.html at root of ZIP)
3. No build required — Vercel will serve the static site automatically.

Local run:
1. Open index.html in your browser (double-click) or use a simple static server (e.g., 'npx serve .')

Features:
- date-based entries (replace if same date added)
- saved in browser localStorage
- export/import JSON, delete entries
- interactive chart (Chart.js CDN)
- minimal animations & responsive layout
